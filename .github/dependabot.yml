version: 2
updates:
  # Watch the nginx base image in the Dockerfile for new releases / CVE fixes.
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"
    labels:
      - "dependencies"
