apiVersion: v1
kind: Service
metadata:
  name: gw-proxy
  namespace: gw-proxy
  labels:
    app: gw-proxy
spec:
  type: ClusterIP
  selector:
    app: gw-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
